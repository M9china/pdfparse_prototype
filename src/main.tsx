import React from 'react'
import ReactDOM from 'react-dom/client'
import { PdfExtractor } from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PdfExtractor />
  </React.StrictMode>,
)
